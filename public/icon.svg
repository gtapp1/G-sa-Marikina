<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" fill="none">
  <!-- Warm cream field so the icon reads on any OS surface -->
  <rect width="512" height="512" fill="#FFFBF5"/>
  <!-- G! badge — the brand mark, centered and square-safe -->
  <rect x="106" y="106" width="300" height="300" rx="28" fill="#EB1700"/>
  <text x="256" y="332" font-family="'Segoe UI', system-ui, sans-serif" font-size="190" font-weight="800" fill="#FFFFFF" text-anchor="middle" letter-spacing="-6">G!</text>
</svg>
